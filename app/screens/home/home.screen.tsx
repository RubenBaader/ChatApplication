import { SafeAreaView } from "react-native-safe-area-context"
import { Text } from "react-native"

export const HomeScreen : React.FC = () => {
    return (
        <SafeAreaView>
            <Text>I'm HOOOOOOME</Text>
        </SafeAreaView>
    )
}